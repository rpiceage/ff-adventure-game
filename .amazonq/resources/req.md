# Create new ff application

## Requirements
- Desktop application in Java
- Handles a yaml document as input, like the requirements/sample.yaml
- Prints the text on a GUI
- Handles the choices with buttons